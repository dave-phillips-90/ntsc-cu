[project]
name = "ntsc"
version = "0.1.0"
description = "NTSC/VHS video artifact emulation for images"
requires-python = ">=3.10"
dependencies = [
    "numpy>=2.2.6",
    "opencv-python>=4.13.0.92",
    "scipy>=1.15.3",
]
